import { tv } from 'tailwind-variants';

export const formVariants = tv({
  slots: {
    input:
      'flex items-center gap-x-2 px-3 py-1.5 bg-primary-foreground rounded-full text-input border border-input'
  }
});
